{
  "name": "srm-students-portal",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspaces",
    "start": "PLAYWRIGHT_BROWSERS_PATH=0 node backend/dist/index.js",
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
